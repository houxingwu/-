
server:
  port: 9527
spring:
  application:
    name: zuul-server
zuul:
  ignoredServices: '*'
  routes:
    plat:
      path: /plat/** #平台服务的请求路径
      serviceId: plat-service
    plat:
      path: /product/** #产品服务的请求路径
      serviceId: product-service
  prefix: /services #请求前缀
